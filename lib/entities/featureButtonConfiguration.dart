import 'package:flutter/material.dart';


class FeatureButtonConfiguration {
  FeatureButtonConfiguration(Color this.color, Icon this.icon, String this.text, VoidCallback this.onClick);
  Color color;
  Icon icon;
  String text;
  VoidCallback onClick; 
}
 

